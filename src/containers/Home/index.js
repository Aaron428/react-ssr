import React from 'react';

const Home = () => {
  return (
    <div>
      <h2>React ssr Home Page!</h2>
      <button onClick={() => alert('you clicked!')}>click me!</button>
    </div>
  );
};

export default Home;
